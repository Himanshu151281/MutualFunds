# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Local MongoDB (default)
MONGODB_URI=mongodb://localhost:27017/mutual-fund-compass

# MongoDB Atlas (for production - replace with your connection string)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/mutual-fund-compass?retryWrites=true&w=majority

# JWT Configuration
# IMPORTANT: Change this to a secure random string in production!
# You can generate one at: https://www.allkeysgenerator.com/Random/Security-Encryption-Key-Generator.aspx
JWT_SECRET=g4e7*Qz@9pLy2kX!7mRPzW5vN8sMK3tY
JWT_EXPIRE=30d

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000,http://localhost:5173

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000        # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100        # Maximum requests per window

# Email Configuration (for future email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# External API Configuration (for mutual fund data)
# MFAPI_BASE_URL=https://api.mfapi.in
# MFAPI_RATE_LIMIT=60            # Requests per minute

# Logging Configuration
# LOG_LEVEL=info
# LOG_FILE=./logs/app.log
